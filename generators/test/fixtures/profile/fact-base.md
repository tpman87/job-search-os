<!-- SYNTHETIC FIXTURE — fully fake, safe to commit. Used only by generators/test/run-tests.js.
     Never point this at a real profile/fact-base.md (JOBS-ADR-004 D15: CI fixtures must be
     synthetic, never adapted from a real resume). Uses the repo's own "obviously fake
     placeholder" convention (see hooks/pre-commit) so this fixture doesn't trip its own
     scrub gate — a real-shaped phone number or linkedin.com URL would, even a fake one. -->
- Name: Pat Testworthy
- Phone: XXX-XXX-XXXX
- Email: pat.testworthy@example.com
- LinkedIn: example.com/in/pat-testworthy
- Location: Testville, ZZ
