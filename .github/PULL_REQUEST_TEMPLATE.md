## What this changes

## Why

## ⚠️ Confirm before submitting
- [ ] This PR contains no real content from anyone's `profile/`, `pipeline/`, or `roles/`
      directories — no real comp figures, employer names, or personal data, including in
      examples or test fixtures. See `CONTRIBUTING.md`.
- [ ] If this touches `docs/LESSONS.md` or `docs/METHOD.md`, any new example follows the
      same "no real identifying detail" discipline as the existing content.
- [ ] If this touches a skill, I tested it against a throwaway/synthetic profile, not a
      real one.
